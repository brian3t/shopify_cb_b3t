function pull_from_abcd(){
  /*let a = 1
  let btn = Event.target
  let cust_tag = $('li.article__tags-item > a').toArray().filter(a =>
    (a.text.includes('cust_')))
    .map(a => a.text).pop()*/

}

(function (){
  jQuery(document).ready(function ($){
    console.log('page blog loaded')

  });

})()
